# 快速开始

## 一、引用

`Runtime/Fang.Framework.asmdef` 的 `autoReferenced` 为 `true`，`Assets/` 下的预定义程序集（`Assembly-CSharp`）可直接使用：

```csharp
using Fang.Framework;
```

如果代码位于自定义 asmdef 中，需要在 asmdef 的 `references` 里加上 `Fang.Framework`。

## 二、安装扩展包

扩展包与核心包同仓，走 **git URL + `?path=` + tag** 分发，需要本机 git CLI。

**方式一：安装窗口**（推荐）

`Tools/Fang Framework/Extension Packages` → 点「刷新索引」→ 点「安装」。

窗口会显示每行的索引版本与本地版本，并给出安装 / 更新 / 卸载按钮；核心包未装时按钮禁用（扩展包依赖核心包）。

**方式二：手写 `Packages/manifest.json`**

```json
{
  "dependencies": {
    "com.fang.framework": "https://github.com/fangzw12138/FangFrameWork.git?path=/Packages/com.fang.framework#com.fang.framework/v0.5.0",
    "com.fang.framework.eventbus": "https://github.com/fangzw12138/FangFrameWork.git?path=/Packages/com.fang.framework.eventbus#com.fang.framework.eventbus/v0.1.0"
  }
}
```

两个要点：

- **Unity 不检测 git 依赖更新**，升级要改 URL 里的 tag（或重新 `Client.Add` 新 tag）—— 这就是窗口存在的理由。
- **扩展包的 `package.json` 里 `dependencies` 是空对象**：UPM 不支持包内声明 git URL 依赖，依赖靠 asmdef 引用 + 窗口前置检查落地。

完整机制、tag 约定、索引格式见 `扩展包分发.md`。第一个扩展包 `com.fang.framework.eventbus` 的用法见其 `README.md` 与 `Packages/com.fang.framework.eventbus/Documentation~/事件总线.md`。

## 三、定义领域

```csharp
using Fang.Framework;
using UnityEngine;

public sealed class ItemConfigDataSo : ConfigDataSo
{
    [SerializeField] private int _maxStack;
    [SerializeField] private int _baseDamage;

    public int MaxStack => _maxStack;
    public int BaseDamage => _baseDamage;
}

public sealed class ItemData : Data<ItemConfigDataSo>
{
    private int _count;
    private int _durability;

    public ItemData(ItemConfigDataSo config) : base(config)
    {
        _count = 1;
        _durability = config.BaseDamage;
    }

    public int Count => _count;
    public int Durability => _durability;

    public void Add(int amount)
    {
        _count = Mathf.Clamp(_count + amount, 0, Config.MaxStack);
    }

    public void ApplyDamage(int amount)
    {
        _durability = Mathf.Max(0, _durability - amount);
    }
}

public sealed class ItemController : Controller<ItemConfigDataSo, ItemData>
{
    public void Use(int amount)
    {
        Data.ApplyDamage(amount);
    }
}
```

要点：

- 配置字段是 `private [SerializeField]`，对外只有只读属性。写配置走 `SerializedObject` / `SerializedProperty` + `EditorUtility.SetDirty`。
- `Data` 只有带参构造，`Config` 在构造时就位。
- `Data` 自己的状态由自己的方法改（`Add` / `ApplyDamage`），不变量在方法里守。
- **`Data` 是纯 C# 类；`Controller` 是 `MonoBehaviour`**，要挂到 GameObject 上。
- `Controller` 不能 `new`：用 `AddComponent<ItemController>()` 创建，再 `Initialize(data)` 与 `Inject(scope)`。

表现层**不在核心包里**：视觉脚本是普通 `MonoBehaviour`，由控制器自己在 `OnInit()` 里创建并 `Initialize(this)`。写法见 `表现层规范.md`。

## 四、定义能力

```csharp
using System.Collections.Generic;
using Fang.Framework;

public sealed class InventoryService : Service
{
    private readonly List<ItemData> _items = new List<ItemData>();

    public override void OnInit()
    {
    }

    public override void OnDispose()
    {
        _items.Clear();
    }

    public void Add(ItemData item)
    {
        _items.Add(item);
    }
}
```

`Service` 是 `MonoBehaviour`，但**不参与 Unity 生命周期**：它不写 `Awake` / `Update` / `OnDestroy`，也不自注册。`Scope` 负责创建它、给它 `Inject(Scope)`、调它的 `OnInit` / `OnDispose`。

`Service` 本身没有抽象成员，只提供 `OnInit` / `OnDispose` 的空实现和注入好的 `Scope`。需要每帧能力的服务自己挂接口：

```csharp
public sealed class SpawnerService : Service, ITickable
{
    public void OnTick(float deltaTime)
    {
    }
}
```

`OnInit` / `OnDispose` 是 `virtual`，覆盖时才写 `override`。

## 五、定义 Scope

`Scope` 是抽象 `MonoBehaviour`，实现 `ILifecycle`。`AddService` / `CreateChildScope` / `RemoveService` 是 `protected` —— 装配由具体 `Scope` 子类负责，外部只通过 `GetService<T>()` 取用。

装配写在 `OnInit()` override 里，**第一件事是 `base.OnInit()`**：

```csharp
using Fang.Framework;

public sealed class GameScope : Scope
{
    public InventoryService Inventory { get; private set; }

    public override void OnInit()
    {
        base.OnInit();

        Inventory = AddService<InventoryService>();
        AddService<SpawnerService>();
    }
}
```

`AddService<T>()` 一步完成创建与托管：建一个名为类型名的物体 → 挂到 Scope 的子物体 `Services` 下 → `AddComponent<T>()` → `Inject(this)` → 入表 → `OnInit()`。

层级长这样：

```text
GameScope                ← 使用方自己建的物体，挂 GameScope 组件
└── Services             ← 懒建
    ├── InventoryService
    └── SpawnerService
```

`IsInitialized` 记录是否已经 `OnInit()` 过。**它只是状态查询，不是门槛**：`AddService` / `RemoveService` / `GetService` 都不检查它，也没有 `Build()` 阶段。

## 六、驱动

框架**不提供宿主 MonoBehaviour**，也不写任何 Unity 消息方法，使用方自己写：

```csharp
using Fang.Framework;
using UnityEngine;

public sealed class GameScopeHost : MonoBehaviour
{
    private GameScope _scope;

    private void Awake()
    {
        _scope = gameObject.AddComponent<GameScope>();
        _scope.OnInit();
    }

    private void Update()
    {
        _scope.Tick(Time.deltaTime);
    }

    private void FixedUpdate()
    {
        _scope.FixedTick(Time.fixedDeltaTime);
    }

    private void OnDestroy()
    {
        _scope.OnDispose();
    }
}
```

- `Tick` / `FixedTick`：先自己的服务（按添加顺序），再递归子 Scope。
- `OnDispose`：先逆序释放全部子 Scope，再逆序 `OnDispose` 自己的服务。幂等（未 `OnInit` 时直接返回）。
- **框架不销毁任何 GameObject**：`OnDispose` 只注销与回调，`Services` 下的物体与子 Scope 物体留给使用方或 Unity 层级回收。
- 所有 Unity 接触面（`Instantiate`、`AudioSource`、协程）都在这一层桥接。

## 七、子 Scope

```csharp
public sealed class BattleScope : Scope
{
    public override void OnInit()
    {
        base.OnInit();

        AddService<BattleService>();
    }
}

public sealed class GameScope : Scope
{
    public BattleScope Battle { get; private set; }

    public override void OnInit()
    {
        base.OnInit();

        Battle = CreateChildScope<BattleScope>();
        Battle.OnInit();
    }
}
```

树形与解析：

```text
GameScope
├── Services
│   ├── InventoryService
│   └── SpawnerService
└── BattleScope            ← CreateChildScope 建的子物体
    └── Services
        └── BattleService
```

- `CreateChildScope<T>()` 在父 Scope 物体下建子物体并 `AddComponent<T>()`，解析链记的是显式 `_parent`。
- 子 Scope 的 `OnInit()` 要由父自己调（`CreateChildScope` 不代劳）。
- 本层 miss → 沿 `_parent` 一路向上，任意一层命中即返回。
- 本层命中就不再向上：`GetService<T>()` 取的是**离自己最近**的那一个。
- **父 `OnDispose()` 会递归释放全部子 Scope**（先逆序子，再逆序自己的服务）。

## 八、服务之间怎么互相取用

`Service` 与 `Controller` 都持有注入好的 `protected Scope`，直接调 `GetService<T>()`：

```csharp
public sealed class BattleService : Service, ITickable
{
    private InventoryService _inventory;

    public override void OnInit()
    {
        _inventory = Scope.GetService<InventoryService>();
    }

    public void OnTick(float deltaTime)
    {
    }
}
```

**零反射**：框架不按构造函数解析依赖，取用一律显式写 `Scope.GetService<T>()`。服务也不在自己的构造函数里注册自己。

装了 `com.fang.framework.eventbus` 后，跨对象的一次性命令走 `Scope.GetService<EventBus>()`：

```csharp
using Fang.Framework.EventBus;

public sealed class BattleService : Service
{
    private EventBus _events;

    public override void OnInit()
    {
        _events = Scope.GetService<EventBus>();
    }

    public void DealDamage(int amount)
    {
        _events.Publish(new DamageDealt(amount));
    }
}
```

## 九、异常

| 异常 | 触发场景 |
| --- | --- |
| `InvalidOperationException` | `GetService<T>()` 沿整条父链都没命中；消息里带服务类型全名与 Scope 名 |
| `ArgumentNullException` | `Controller.Initialize(null)` |

核心层不再定义自定义异常类型。

## 十、明确不做

- 核心包不提供 `Entry` 启动入口、日志抽象、事件总线、资源管理、对象池。
- 事件总线与对象池是**扩展包**：事件总线见 `com.fang.framework.eventbus`（`Packages/com.fang.framework.eventbus/Documentation~/事件总线.md`），对象池待做。
- 框架不提供宿主 MonoBehaviour，也不写 `Awake` / `Update` / `OnDestroy`。
- 框架不做序列化 / 存档。
- 框架不销毁 GameObject，也不提供实体挂载原语（没有 `Scope.Mount` / `OnAttach` / `OnDetach`）。
- `Controller` 的 tick 驱动者未定，取决于「实体挂载」的设计。
